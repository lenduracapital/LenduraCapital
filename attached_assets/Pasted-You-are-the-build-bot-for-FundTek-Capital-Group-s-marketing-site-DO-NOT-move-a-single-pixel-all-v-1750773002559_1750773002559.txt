You are the build-bot for FundTek Capital Group’s marketing site.
DO NOT move a single pixel; all visual styling and copy layout stay exactly as-is.

Commit everything below in one pull request titled
“🔒 SEO • Perf • Security • Analytics hardening – NO design change”.

────────────────────────────────────────────────────────
CORE CLEAN-UP
 1. Strip junk: add .replitignore → .git, .config, node_global, Pulse*, npm cache, *.zip.
 2. Use hashed build output filenames (e.g. main.[hash].js) + automatic cache-bust.

PERFORMANCE
 3. Self-host Inter (400-700) → /assets/fonts; preload woff2 + font-display:swap.
 4. Purge unused CSS (PurgeCSS against /src, safelist dynamic classes).
 5. Tree-shake & minify JS with esbuild (target ES2020; drop console.* & debugger).
 6. Enable Brotli + Gzip in Express `compression()` middleware.

SEMANTIC / ACCESSIBILITY
 7. Add lang="en" to <html>, role="main" landmarks, tabindex management.
 8. Headings: <h1> on hero, <h2> per section. aria-label decorative div headings.
 9. All <img> get meaningful alt; purely decorative → role="presentation".
10. Ensure contrast ≥ 4.5:1; darken #85ABE4 by 5 %.

SEO
11. One  160-char meta description & canonical link per page.
12. JSON-LD in <head>:
      • Organization (logo, tel, sameAs socials)  
      • FAQPage (8 Q&A)  
      • Review (3 testimonials, rating 5/5)  
      • BreadcrumbList
13. robots.txt:  
      User-agent: *  
      Allow: /  
      Sitemap: /sitemap.xml
14. sitemap.xml auto-generates each build (includes hidden pages).

ASSET OPTIMISATION
15. Video-FundTek.webm first, MP4 fallback; lazy-load via <video> poster attr.
16. Convert hero JPGs → WebP/AVIF, add explicit width/height + loading="lazy".
17. Inline critical CSS ≤ 15 KB; remainder deferred.

SECURITY
18. Helmet middleware with:  
      • Strict-Transport-Security: max-age = 63072000; includeSubDomains; preload  
      • Content-Security-Policy: default-src 'self'; img-src 'self' data: https:; frame-src https://form.jotform.com; connect-src 'self' https://www.google-analytics.com  
      • X-Content-Type-Options: nosniff  
      • X-Frame-Options: SAMEORIGIN  
      • Referrer-Policy: strict-origin-when-cross-origin
19. Rate-limit forms: 100 requests / 15 min per IP (express-rate-limit).

PWA & OFFLINE HANDLING
20. manifest.webmanifest → short_name “FundTek”, theme_color “#85abe4”, display "standalone".
21. service-worker:  
      • Cache-first for fonts/images; network-first for HTML/API.  
      • Offline fallback page (offline.html).  
      • Log sw fetch errors via gtag event once/session.

FORMS / CTA
22. Override JotForm iframe buttons with global style:  
      button,[type=submit]{background:#85abe4;color:#fff;transition:filter .2s}  
      button:hover{filter:brightness(.92)}  
      button:focus-visible{outline:2px solid #000}
23. Add hidden checkbox + label for SMS opt-in; name="sms_opt_in" value="yes".

ANALYTICS (LIVE)
24. Inject at top of <head> on every page (no placeholders):

   <!-- Google tag (gtag.js) -->
   <script async src="https://www.googletagmanager.com/gtag/js?id=G-C5XF22RXMM"></script>
   <script>
     window.dataLayer = window.dataLayer || [];
     function gtag(){dataLayer.push(arguments);}
     gtag('js', new Date());
     gtag('config','G-C5XF22RXMM',{send_page_view:false});
   </script>

25. For single-page navigation: on route change →  
    gtag('config','G-C5XF22RXMM',{page_path:window.location.pathname});

BUILD & TEST
26. npm run build && npm start → run lighthouse-ci: mobile ≥ 95, CLS < 0.1, LCP < 2.5 s, Best-Practices ≥ 95, Security headers present.
27. Autodeploy preview URL comment on PR.

Deliver exactly one commit. No visual or wording changes beyond the steps above.
────────────────────────────────────────────────────────
