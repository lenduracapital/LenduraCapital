import { useEffect } from "react";

// Google Analytics 4 initialization
export default function Analytics() {
  useEffect(() => {
    // GA4 is already initialized in index.html
    // This component is kept for potential future analytics needs
  }, []);

  return null;
}